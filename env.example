# Copy to .env next to docker-compose.yml:  cp env.example .env
# Then edit passwords before: docker compose up -d --build

MYSQL_ROOT_PASSWORD=change_this_root_password
MYSQL_DATABASE=stc_associate_go
MYSQL_USER=stc_app
MYSQL_PASSWORD=change_this_app_password
